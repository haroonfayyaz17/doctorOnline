import 'package:flutter/material.dart';

class WColors {
  Color appBar;
  Color textColor;
  Color appBarText;
  Color appBarIcons;
  Color bottomBarIcons;

  WColors() {
    appBarIcons = Color(0xFFFFFFFF);
    appBarText = Color(0xFFFFFFFF);
    appBar = Color(0xFF00AAF7);
    textColor = Color(0x00000000);
    bottomBarIcons=Colors.grey;
  }
}
